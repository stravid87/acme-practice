apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: my-org-cluster-issuer
spec:
  ca:
    secretName: root-ca-secret
    # IMPORTANT: Since this is a ClusterIssuer, it looks for 
    # this secret in the namespace where cert-manager is installed.